import { Canvas, Meta, Story } from '@storybook/addon-docs';

import Menu from './index.tsx';

<Meta title="Patterns/Data Display/Menu" component={Menu} />

export const Template = ({ title }) => {
  const dummyOnClickHandlerFactory = (value) => {
    return () => {
      alert(`${value} clicked`);
    };
  };
  const menuItems = [
    { text: 'Detusch', onClick: dummyOnClickHandlerFactory('Detusch') },
    { text: 'English', onClick: dummyOnClickHandlerFactory('English') },
    { text: 'Français', onClick: dummyOnClickHandlerFactory('Français') },
    { text: 'Italiano', onClick: dummyOnClickHandlerFactory('Italiano') },
  ];
  return <Menu title={title} items={menuItems} />;
};

# Overview

<Canvas>
  <Story
    name="Overview"
    args={{
      title: 'Lanague',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
