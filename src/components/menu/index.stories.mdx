import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';

import Menu from './index.tsx';

<Meta title="Patterns/Data Display/Menu" component={Menu} />

export const Template = ({ title }) => {
  const menuItems = [
    { text: 'Detusch', onClick: action('Detusch') },
    { text: 'English', onClick: action('English') },
    { text: 'Français', onClick: action('Français') },
    { text: 'Italiano', onClick: action('Italiano') },
  ];
  return <Menu title={title} items={menuItems} />;
};

# Overview

<Canvas>
  <Story
    name="Overview"
    args={{
      title: 'Lanague',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
