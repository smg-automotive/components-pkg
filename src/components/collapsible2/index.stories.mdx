import { ArgsTable, Meta, Story } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';

import { IconButton } from '@chakra-ui/react';

import { ChevronDownLargeIcon } from '../icons';
import Box from '../box';

import Collapsible2Template from './StoryTemplate.tsx';

import Collapsible2 from './index';

<Meta
  title="Components/Filter/Collapsible2"
  argTypes={{
    onApplyAction: {
      table: {
        disable: true,
      },
    },
    defaultFacets: {
      table: {
        disable: true,
      },
    },
    image: {
      table: {
        disable: true,
      },
    },
  }}
/>

export const Template = (args) => {
  return (
    <Box w="8xl">
      <Collapsible2Template {...args} />
    </Box>
  );
};

# Collapsible2

<ArgsTable of={Collapsible2} />

## Overview

<Story
  name="Overview"
  args={{
    onApplyAction: action('onApply'),
  }}
>
  {Template.bind({})}
</Story>

## Collapsible2

<Story
  name="Collapsible2"
  args={{
    onApplyAction: action('onApply'),
    numberOfColumnsOnDesktop: 3,
    isCollapsible: true,
    isParent: true,
    icon: (
      <IconButton
        icon={
          <ChevronDownLargeIcon
            w="xs"
            h="xs"
            transition="0.2s"
            color="gray.500"
            marginLeft={20}
            boxSize={18}
            cursor="pointer"
          />
        }
      />
    ),
  }}
>
  {Template.bind({})}
</Story>
