import { Canvas, Meta, Story } from '@storybook/addon-docs';

import Count from './index.tsx';

<Meta
  title="Components/Data display/Count"
  component={Count}
  argTypes={{
    variant: {
      type: 'select',
      options: ['primary', 'inverted'],
    },
  }}
/>

export const Template = (args) => <Count {...args} />;

# Count

<Canvas>
  <Story
    name="Count"
    args={{
      variant: 'primary',
      count: '50',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
