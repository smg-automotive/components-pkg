import { Canvas, Meta, Story } from '@storybook/addon-docs';

import Button from './index.tsx';

<Meta title="Components/Button" component={Button} />

export const Template = (args) => <Button {...args}>Primary button</Button>;

# Button

<Canvas>
  <Story
    name="Basic"
    args={{
      isDisabled: false,
    }}
    argTypes={{
      variant: {
        options: ['primary'],
        control: { type: 'radio' },
        defaultValue: 'primary',
      },
      size: {
        options: ['md', 'lg', 'xl'],
        control: { type: 'radio' },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Disabled"
    args={{
      isDisabled: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
