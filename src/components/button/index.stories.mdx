import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import Button from './index.tsx';
import { Stack } from '../../index.ts';

<Meta title="Buttons/Button" component={Button} />

export const Template = (args) => (
  <Button {...args}>{args.label || 'Button'}</Button>
);

# Button

## Primary

<Canvas>
  <Story
    name="Primary"
    args={{
      isDisabled: false,
      label: 'Primary Button',
      variant: 'primary',
      size: 'lg',
    }}
    argTypes={{
      variant: {
        options: ['primary', 'secondary'],
        control: 'select',
      },
      size: {
        options: ['md', 'lg'],
        control: 'select',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Primary" />

## Secondary

<Canvas>
  <Story
    name="Secondary"
    args={{
      isDisabled: false,
      label: 'Secondary Button',
      variant: 'secondary',
      size: 'lg',
    }}
    argTypes={{
      variant: {
        options: ['primary', 'secondary'],
        control: 'select',
      },
      size: {
        options: ['md', 'lg'],
        control: 'select',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Secondary" />

## Sizes

<Canvas>
  <Story name="Sizes">
    <Stack spacing="lg" direction="row">
      <Button size="md">Medium</Button>
      <Button size="lg">Large</Button>
    </Stack>
  </Story>
</Canvas>

## Disabled

<Canvas>
  <Story name="Disabled">
    <Stack spacing="lg" direction="row">
      <Button isDisabled={true}>Primary Button</Button>
      <Button variant="secondary" isDisabled={true}>
        Secondary Button
      </Button>
    </Stack>
  </Story>
</Canvas>

## Custom component into Link

<Canvas>
  <Story
    name="Custom component into Link"
    args={{
      isDisabled: false,
      label: 'Primary Button',
      variant: 'primary',
      size: 'lg',
      customComponent: 'link',
    }}
    argTypes={{
      variant: {
        options: ['primary', 'secondary'],
        control: 'select',
      },
      size: {
        options: ['md', 'lg'],
        control: 'select',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Custom component into Link" />
