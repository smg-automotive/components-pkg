import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import { Stack } from 'src/index.ts';

import { ShareIcon, VideoIcon } from '../index.ts';
import Button from './index.tsx';

<Meta title="Components/Button" component={Button} />

export const Template = (args) => <Button {...args}>{args.label}</Button>;

# Button

## Primary

<Canvas>
  <Story
    name="Primary"
    args={{
      isDisabled: false,
      label: 'Primary Button',
      variant: 'primary',
      size: 'lg',
    }}
    argTypes={{
      variant: {
        options: ['primary', 'secondary'],
        control: 'select',
      },
      size: {
        options: ['md', 'lg'],
        control: 'select',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Primary" />

## Secondary

<Canvas>
  <Story
    name="Secondary"
    args={{
      isDisabled: false,
      label: 'Secondary Button',
      variant: 'secondary',
      size: 'lg',
    }}
    argTypes={{
      variant: {
        options: ['primary', 'secondary'],
        control: 'select',
      },
      size: {
        options: ['md', 'lg'],
        control: 'select',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Secondary" />

## Sizes

<Canvas>
  <Story name="Sizes">
    <Stack spacing="lg" direction="row">
      <Button size="md">Medium</Button>
      <Button size="lg">Large</Button>
    </Stack>
  </Story>
</Canvas>

## Disabled

<Canvas>
  <Story name="Disabled">
    <Stack spacing="lg" direction="row">
      <Button isDisabled={true}>Primary Button</Button>
      <Button variant="secondary" isDisabled={true}>
        Secondary Button
      </Button>
    </Stack>
  </Story>
</Canvas>

## Button as Link

<Canvas>
  <Story
    name="Button as Link"
    args={{
      label: 'Primary Button',
      variant: 'primary',
      size: 'lg',
      as: 'a',
      href: 'https://google.com',
      isExternal: true,
    }}
    argTypes={{
      variant: {
        options: ['primary', 'secondary'],
        control: 'select',
      },
      size: {
        options: ['md', 'lg'],
        control: 'select',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Button as Link" />

## Button with left icon

<Canvas>
  <Story
    name="Button with left icon"
    args={{
      label: 'Button with left icon',
      variant: 'primary',
      size: 'lg',
      leftIcon: <VideoIcon />,
    }}
    argTypes={{
      variant: {
        options: ['primary', 'secondary'],
        control: 'select',
      },
      size: {
        options: ['md', 'lg'],
        control: 'select',
      },
      leftIcon: {
        table: {
          disable: true,
        },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Button with left icon" />

## Button with right icon

<Canvas>
  <Story
    name="Button with right icon"
    args={{
      label: 'Button with right icon',
      variant: 'primary',
      size: 'lg',
      rightIcon: <VideoIcon />,
    }}
    argTypes={{
      variant: {
        options: ['primary', 'secondary'],
        control: 'select',
      },
      size: {
        options: ['md', 'lg'],
        control: 'select',
      },
      rightIcon: {
        table: {
          disable: true,
        },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Button with right icon" />

## Icon button

<Canvas>
  <Story
    name="Icon button"
    args={{
      variant: 'primary',
      size: 'lg',
      icon: <ShareIcon />,
      ariaLabel: 'Share to Facebook',
    }}
    argTypes={{
      variant: {
        options: ['primary', 'secondary'],
        control: 'select',
      },
      size: {
        options: ['md', 'lg'],
        control: 'select',
      },
      icon: {
        table: {
          disable: true,
        },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Icon button" />
