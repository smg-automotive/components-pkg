import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';

import { Box } from '@chakra-ui/react';

import SimpleHeading from './index.tsx';

<Meta title="Navigation/Simple Heading" component={SimpleHeading} />

export const Template = (args) => <SimpleHeading {...args} />;

## Simple Heading

<Canvas>
  <Story
    name="Simple Heading"
    args={{
      title: 'Nachricht an den Verkäufer',
      onclick: () => action('close'),
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Simple Heading" />

