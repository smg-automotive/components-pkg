import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import ButtonLink from './index.tsx';
import { Stack } from '../../index.ts';

<Meta title="Buttons/Button as Link" component={ButtonLink} />

export const Template = (args) => (
  <ButtonLink {...args}>{args.label || 'Button'}</ButtonLink>
);

# Button

## Overview

<Canvas>
  <Story
    name="Overview"
    args={{
      label: 'Primary Button',
      variant: 'primary',
      size: 'lg',
      url: 'https://www.autoscout24.ch/de',
    }}
    argTypes={{
      variant: {
        options: ['primary', 'secondary'],
        control: 'select',
      },
      size: {
        options: ['md', 'lg'],
        control: 'select',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Overview" />
