import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import ButtonLink from './index.tsx';
import { Stack } from '../../index.ts';

<Meta title="Buttons/Button as Link" component={Button} />

export const Template = (args) => (
  <ButtonLink {...args}>{args.label || 'Button'}</ButtonLink>
);

# Button as Link

## Primary

<Canvas>
  <Story
    name="Primary"
    args={{
      isDisabled: false,
      label: 'Primary Button',
      variant: 'primary',
      size: 'lg',
      as: 'a',
      href='https://google.com',
    }}
    argTypes={{
      variant: {
        options: ['primary', 'secondary'],
        control: 'select',
      },
      size: {
        options: ['md', 'lg'],
        control: 'select',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Primary" />

## Secondary

<Canvas>
  <Story
    name="Secondary"
    args={{
      isDisabled: false,
      label: 'Secondary Button',
      variant: 'secondary',
      size: 'lg',
      as: 'a',
      href='https://google.com',
    }}
    argTypes={{
      variant: {
        options: ['primary', 'secondary'],
        control: 'select',
      },
      size: {
        options: ['md', 'lg'],
        control: 'select',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Secondary" />
