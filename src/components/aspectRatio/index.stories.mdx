import { Canvas, Meta, Story } from '@storybook/addon-docs';

import Box from '../box';
import AspectRatio from './index.tsx';

<Meta title="Layout/Aspect ratio" component={AspectRatio} />

export const Template = (args) => (
  <AspectRatio {...args}>
    <Box backgroundColor="red.500">I am in the box with a ratio</Box>
  </AspectRatio>
);

# AspectRatio

## Overview

<Canvas>
  <Story
    name="Aspect ratio"
    args={{
      ratio: 4 / 3,
      width: 300,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
