import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import TabPanels from './TabPanels';
import TabPanel from './TabPanel';
import TabList from './TabList';
import Tab from './Tab';

import Tabs from './index';

<Meta title="Components/Navigation/Tabs" component={Tabs} />

export const Template = (args) => (
  <Tabs {...args}>
      <TabList>
        <Tab>One</Tab>
        <Tab {...args}>Two</Tab>
        <Tab>Three</Tab>
        <Tab>Fourth</Tab>
      </TabList>

      <TabPanels>
        <TabPanel>
          <p>One</p>
        </TabPanel>
        <TabPanel>
          <p>Two</p>
        </TabPanel>
        <TabPanel>
          <p>Three</p>
        </TabPanel>
        <TabPanel>
          <p>Fourth</p>
        </TabPanel>
      </TabPanels>
    </Tabs>

);

# Tab

## Overview

<Canvas>
  <Story
    name="Overview"
    args={{
      isDisabled: false,
      defaultIndex: 0,
    }}
    argTypes={{
      variant: {
        options: ['spaceBetween', 'spaceAround'],
        control: 'select',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Overview" />

## With one tab disabled

<Canvas>
  <Story
    name="With one tab disabled"
    args={{
      isDisabled: true,
      defaultIndex: 0,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="With one tab disabled" />

## With second tab selected

<Canvas>
  <Story
    name="With second tab selected"
    args={{
      isDisabled: false,
      defaultIndex: 1,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="With second tab selected" />

## Custom tab variant

This styling is used for homepage tabs on listings-web, therefore the styling in this storybook is not the actual style used in the application.

<Canvas>
  <Story
    name="Custom tab variant"
    args={{
      isDisabled: false,
      defaultIndex: 1,
      variant: 'custom',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="With custom tab variant" />
