import { Canvas, Meta, Story } from '@storybook/addon-docs';

import { H1, H2 } from 'src/components/heading';
import Box from 'src/components/box';

import GalleryHeader from './index';

<Meta title="Components/Navigation/GalleryHeader" component={GalleryHeader} />

export const Template = (args) => (
  <Box backgroundColor="black">
    <GalleryHeader
      currentSlide={2}
      slidesCount={3}
      onClose={() => null}
      {...args}
    >
      <>
        <H1 textStyle="body-large">Title</H1>
        <H2 textStyle="body-large">Subtitle</H2>
      </>
    </GalleryHeader>
  </Box>
);

# GalleryHeader

## Overview

<Canvas>
  <Story
    name="GalleryHeader"
    args={{
      title: 'Gallery Header',
      subtitle: 'Subtitle',
      language: 'de',
    }}
    argTypes={{
      language: {
        options: ['de', 'fr', 'it', 'en'],
        control: 'select',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
