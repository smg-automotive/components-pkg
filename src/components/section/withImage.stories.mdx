import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import Section from './index.tsx';

<Meta
  title="Patterns/Sections/WithImage"
  component={Section}
  args={{
    title:
      'Thank you for sending a message! We have sent a copy to your inbox.',
    text: 'Auto-Center Grenchen AG has received your message. A common response time is around 1 to 4 days.',
    nativeImageSize: '320x320',
    maxImgW: '2xl',
  }}
  argTypes={{
    variant: {
      options: ['hero', 'regular'],
      control: 'select',
    },
    maxImgW: {
      options: ['xl', '2xl', '3xl', '4xl', '5xl', '6xl', '7xl'],
      control: 'select',
    },
  }}
/>

export const Template = ({ nativeImageSize, ...args }) => (
  <Section
    {...args}
    image={<img src={`https://via.placeholder.com/${nativeImageSize}`} />}
  />
);

# Sections

## With Image

### Hero

<Canvas>
  <Story
    name="Hero"
    args={{
      variant: 'hero',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Hero" />

### Hero Responsive

Bigger image on mobile and tablet viewports.

<Canvas>
  <Story
    name="Hero Responsive"
    args={{
      variant: 'hero',
      maxImgW: { sm: 'full', lg: '3xl' },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Hero Responsive" />

### Regular

<Canvas>
  <Story
    name="Regular"
    args={{
      variant: 'regular',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Regular" />

### Regular Responsive

Bigger image on mobile and tablet viewports.

<Canvas>
  <Story
    name="Regular Responsive"
    args={{
      variant: 'regular',
      maxImgW: { sm: 'full', lg: '3xl' },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Regular Responsive" />
