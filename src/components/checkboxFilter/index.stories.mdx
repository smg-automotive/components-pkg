import { Meta, Story } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';

import Box from '../box';
import CheckboxFilter from './StoryTemplate.tsx';

<Meta
  title="Components/Filter/Checkbox"
  argTypes={{
    onApplyAction: {
      table: {
        disable: true,
      },
    },
    defaultFacets: {
      table: {
        disable: true,
      },
    },
  }}
/>

export const Template = (args) => {
  return (
    <Box w="6xl">
      <CheckboxFilter {...args} />
    </Box>
  );
};

# Checkbox

## Overview

<Story
  name="Overview"
  args={{
    onApplyAction: action('onApply'),
  }}
>
  {Template.bind({})}
</Story>

## With disabled

<Story
  name="With disabled"
  args={{
    onApplyAction: action('onApply'),
    defaultFacets: { 'old-timer': 0 },
  }}
>
  {Template.bind({})}
</Story>
