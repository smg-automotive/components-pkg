import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import VehicleDataGroupField from './VehicleDataGroupField.tsx';
import { CloseIcon } from '../icons';

import VehicleDataGroup from './';

<Meta
  title="Components/Data Display/VehicleDataGroup"
  component={VehicleDataGroup}
/>

export const ItemLabel = () => (
    <>
    <CloseIcon />
    Some value
    </>
)

export const ItemValue = () => (
  <>
    <CloseIcon />
    Some value
  </>
)

export const Template = (args) => {

const label = 'VehicleDataGroupField label';
const value = 'VehiculeDataGroupField Value';

return (

<VehicleDataGroup {...args} title={args.title} w={args.w}>
  <VehicleDataGroupField label={label} value={value} />
  <VehicleDataGroupField label={label} value={value} />
  <VehicleDataGroupField label={label} value={value} />
</VehicleDataGroup>
) };


export const TemplateWithIconInLabel = (args) => (
  <VehicleDataGroup {...args} title={args.title} w={args.w}>
    <VehicleDataGroupField label={<ItemLabel />} value={<ItemValue />} />
    <VehicleDataGroupField label={<ItemLabel />} value={<ItemValue />} />
    <VehicleDataGroupField label={<ItemLabel />} value={<ItemValue />} />
  </VehicleDataGroup>
);

# GroupLabel

## With title

<Canvas>
  <Story
    name="With title"
    args={{
      title: 'Vehicle Group Data',
      w: '',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="With title" />


## With icon in label and value

<Canvas>
  <Story
    name="With icon in label and value"
    args={{
      title: 'Vehicle Group Data',
      w: '',
    }}
  >
    {TemplateWithIconInLabel.bind({})}
  </Story>
</Canvas>

<ArgsTable story="With icon in label and value" />


## Without title

<Canvas>
  <Story
    name="Without title"
    args={{
      w: '',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Without title" />

