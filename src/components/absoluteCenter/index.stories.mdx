import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { Box } from '@chakra-ui/react';

import AbsoluteCenter from './index.tsx';

<Meta title="Layout/Absolute Center" component={AbsoluteCenter} />

export const Template = (args) => {
  return (
    <Box position="relative" h="400px">
      <AbsoluteCenter {...args}>I am absolute centered</AbsoluteCenter>
    </Box>
  );
};

export const axis = {
  options: ['vertical', 'horizontal', 'both', 'none'],
  control: 'select',
};

# Absolute Center

## With axis

<Canvas>
  <Story
    name="With axis"
    args={{
      padding: '2xl',
      borderColor: 'gray.900',
      borderWidth: '1px',
    }}
    argTypes={{ axis }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## With custom left

<Canvas>
  <Story
    name="With custom left"
    args={{
      padding: '2xl',
      borderColor: 'gray.900',
      borderWidth: '1px',
      left: '4xl',
      axis: 'vertical',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## With custom top

<Canvas>
  <Story
    name="With custom top"
    args={{
      padding: '2xl',
      borderColor: 'gray.900',
      borderWidth: '1px',
      top: '4xl',
      axis: 'horizontal',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
