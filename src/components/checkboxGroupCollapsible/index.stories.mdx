import { ArgsTable, Meta, Story } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';

import Box from '../box';

import CheckboxCollapsibleTemplate from './StoryTemplate.tsx';

import CheckboxCollapsible from './index';

<Meta
  title="Components/Filter/CheckboxCollapsible"
  argTypes={{
    onApplyAction: {
      table: {
        disable: true,
      },
    },
    defaultFacets: {
      table: {
        disable: true,
      },
    },
    onParentCheckboxChange: {
      table: {
        disable: true,
      },
    },
    image: {
      table: {
        disable: true,
      },
    },
  }}
/>

export const Template = (args) => {
  return (
    <Box w="8xl">
      <CheckboxCollapsibleTemplate {...args} />
    </Box>
  );
};

# Checkbox collapsible

<ArgsTable of={CheckboxCollapsible} />

## Overview

<Story
  name="Overview"
  args={{
    onApplyAction: action('onApply'),
    onParentCheckboxChange: action('onParentCheckboxChange'),
    image: (
      <img src="https://placekitten.com/g/220/100" alt="placeholder image" />
    ),
  }}
>
  {Template.bind({})}
</Story>
