import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import FormWithVehicleReference from './FormWithVehicleReference.tsx';

<Meta
  title="Layout/Pages/Form with vehicle reference"
  component={FormWithVehicleReference}
/>

export const Template = (args) => <FormWithVehicleReference {...args} />;

# Form with vehicle

<Canvas>
  <Story
    name="Form with vehicle reference"
    args={{
      title: 'Nachricht an den Verkäufer',
      url: 'https://www.autoscout24.ch/de',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Form with vehicle reference" />
