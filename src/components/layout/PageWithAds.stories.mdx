import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import PageWithAds from './PageWithAds';
import { Box, Center, Divider, Flex, SimpleHeader, Text } from '../index';
import { sizes } from '../../themes/shared/sizes';

<Meta
  title="Layout/Pages/Layout with ads"
  component={PageWithAds}
  args={{
    header: (
      <SimpleHeader
        title="I am a simple header!"
        url="https://www.autoscout24.ch/de"
      />
    ),
    maxContentWidth: 'lg',
    footer: (
      <Box bg="gray.200" h="400px">
        <Divider />
        <Center>
          <Text p="2xl">I am the footer</Text>
        </Center>
      </Box>
    ),
    skyScraperAds: (
      <Flex bg="red.100" h="600px" justifyContent="center" alignItems="center">
        <Text>Ads</Text>
      </Flex>
    ),
    children: (
      <Box bg="green.100" py="md">
        {Array.from({ length: 30 })
          .fill(1)
          .map((el, index) => (
            <Box
              key={index}
              borderWidth="1px"
              borderRadius="lg"
              p="md"
              mb={index !== 29 ? 'md' : 0}
              mx="md"
            >
              <Text>List element {index + 1}</Text>
            </Box>
          ))}
      </Box>
    ),
  }}
  argTypes={{
    header: {
      table: {
        disable: true,
      },
    },
    footer: {
      table: {
        disable: true,
      },
    },
    skyScraperAds: {
      table: {
        disable: true,
      },
    },
    children: {
      table: {
        disable: true,
      },
    },
    maxContentWidth: {
      options: Object.keys(sizes.container),
      control: { type: 'select' },
    },
  }}
  parameters={{
    layout: 'fullscreen',
  }}
/>

export const Template = (args) => <PageWithAds {...args} />;

# Layout with ads

<Canvas>
  <Story name="Layout with ads">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="Layout with ads" />
