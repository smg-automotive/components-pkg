import { Canvas, Meta, Story } from '@storybook/addon-docs';

import MissingImage from './index.tsx';
import AspectRatio from '../aspectRatio';

<Meta title="Patterns/Data display/Missing image" component={MissingImage} />

export const Template = (args) => (
  <AspectRatio ratio={4 / 3} width="500px">
    <MissingImage {...args} />
  </AspectRatio>
);

# Missing image

## Overview

<Canvas>
  <Story name="Overview">{Template.bind({})}</Story>
</Canvas>
