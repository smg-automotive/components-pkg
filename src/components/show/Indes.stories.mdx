import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import Show from './index.ts';

<Meta
  title="Components/Utils/Show"
  component={Show}
  argTypes={{
    below: {
      options: ['2xs', 'xs', 'sm', 'md', 'lg', 'xl'],
      control: { type: 'select' },
    },
    above: {
      options: ['2xs', 'xs', 'sm', 'md', 'lg', 'xl'],
      control: { type: 'select' },
    },
  }}
/>

export const Template = ({ text, ...args }) => {
  return <Show {...args}>{text}</Show>;
};

# Show

## Above a viewport

<Canvas>
  <Story
    name="Above a viewport"
    args={{ above: 'md', text: 'I am visible on md and above' }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Above a viewport" />

## Below a viewport

<Canvas>
  <Story
    name="Below a viewport"
    args={{ below: 'md', text: 'I am visible on md and below' }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Below a viewport" />
