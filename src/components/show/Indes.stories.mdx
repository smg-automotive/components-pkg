import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import Show from './index.tsx';

<Meta
  title="Components/Utils/Show"
  component={Show}
  argTypes={{
    below: {
      options: ['2xs', 'xs', 'sm', 'md', 'lg', 'xl'],
      control: { type: 'select' },
    },
    above: {
      options: ['2xs', 'xs', 'sm', 'md', 'lg', 'xl'],
      control: { type: 'select' },
    },
    text: {
      table: {
        disable: true,
      },
    },
  }}
/>

export const Template = ({ text, ...args }) => {
  const breakpoint = args.below || args.above || '[please pass a breakpoint]';
  return <Show {...args}>{text.replace('BREAKPOINT', breakpoint)}</Show>;
};

# Show

## Show above a viewport (including the passed viewport)

<Canvas>
  <Story
    name="Show above a viewport (including the passed viewport)"
    args={{
      above: 'md',
      text: 'I am visible on BREAKPOINT and above and hidden otherwise',
    }}
    argTypes={{
      below: {
        table: {
          disable: true,
        },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Show above a viewport" />

## Show below a viewport (including the passed viewport)

<Canvas>
  <Story
    name="Show below a viewport (including the passed viewport)"
    args={{
      below: 'md',
      text: 'I am visible on BREAKPOINT and below and hidden otherwise',
    }}
    argTypes={{
      above: {
        table: {
          disable: true,
        },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Show below a viewport" />

<Canvas>
  <Story
    name="Shown when matching a query"
    args={{
      breakpoint:
        '(orientation: landscape) and (hover: none) and (pointer: coarse)',
      text: 'I am only shown on mobile landscape',
    }}
    argTypes={{
      above: {
        table: {
          disable: true,
        },
      },
      below: {
        table: {
          disable: true,
        },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="With a custom breakpoint query" />
