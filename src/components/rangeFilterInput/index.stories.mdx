import { Canvas, Meta, Story } from '@storybook/addon-docs';

import { Box } from '@chakra-ui/react';

import RangeFilterInput from './index';

<Meta title="Components/Forms/RangeFilterInput" component={RangeFilterInput} />;

export const Template = (args) => <RangeFilterInput {...args} />;

## Overview

<Canvas>
  <Story
    name="RangeFilterInput"
    args={{
      unit: 'CHF',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
