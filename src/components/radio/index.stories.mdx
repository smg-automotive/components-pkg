import { useState } from 'react';

import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import { Stack } from 'src/index.ts';

import Radio from './index.tsx';

<Meta title="Components/Forms/Radio" component={Radio} />

export const Template = ({ isChecked, ...args }) => {
  const [checked, setChecked] = useState(isChecked);
  return (
    <Radio
      {...args}
      isChecked={checked}
      onChange={(e) => setChecked(e.target.checked)}
    />
  );
};

# Radio

## Overview

<Canvas>
  <Story
    name="Overview"
    args={{
      isChecked: false,
      isDisabled: false,
      isInvalid: false,
      label: 'Regular',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Overview" />

## Selected

<Canvas>
  <Story
    name="Selected"
    args={{
      isChecked: true,
      isDisabled: false,
      label: 'Selected',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Invalid

<Canvas>
  <Story
    name="Invalid"
    args={{
      isChecked: false,
      isDisabled: false,
      isInvalid: true,
      label: 'Invalid',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Disabled

<Canvas>
  <Story name="Disabled">
    <Stack spacing="lg" direction="row">
      <Radio isDisabled={true} label="Disabled" />
      <Radio isDisabled={true} isChecked={true} label="Disabled checked" />
    </Stack>
  </Story>
</Canvas>
