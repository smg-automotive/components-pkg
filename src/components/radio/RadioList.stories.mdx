import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';

import { Spacer } from '@chakra-ui/react';

import RadioList from './RadioList.tsx';
import { Box, Flex } from '../index';

<Meta
  title="Components/Forms/RadioList"
  component={RadioList}
  parameters={{
    layout: 'fullscreen',
  }}
/>

export const Container = ({ children }) => {
  return (
    <Box padding="xl" background="purple">
      {children}
    </Box>
  );
};

export const DummyOption = (name) => {
  return (
    <Flex key={name}>
      <div>
        <div>A5 Coupé 2.0 40 TDO Sport qu. S-Tronic</div>
        <div>2018 - 2020</div>
      </div>
      <Spacer />
      <div>140 (190)</div>
      <Spacer />
      <div>Automatic</div>
      <Spacer />
      <div>Diesel</div>
      <Spacer />
      <div>3</div>
      <Spacer />
      <div>5</div>
    </Flex>
  );
};

export const Template = (args) => {
  return (
    <Container>
      <RadioList {...args} />
    </Container>
  );
};

# Radio

## List

<Canvas>
  <Story
    name="List"
    args={{
      name: 'Demo',
      onChange: action,
      options: [
        <DummyOption key="one" name="one" />,
        <DummyOption key="two" name="two" />,
        <DummyOption key="three" name="three" />,
      ],
    }}
    argTypes={{
      name: 'Demo',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Overview" />
