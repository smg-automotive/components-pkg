import { Canvas, Meta, Story } from '@storybook/addon-docs';

import HeaderFooterMenuSection from './HeaderFooterMenuSection';

import ListItem from '../list/ListItem';
import List from '../list';

import HeaderFooterMenu from './index';

<Meta
  title="Patterns/Navigation/HeaderFooterMenu"
  component={HeaderFooterMenu}
/>

export const Template = (args) => (
  <HeaderFooterMenu {...args}>
    <HeaderFooterMenuSection title={args.title}>
      {args.children}
    </HeaderFooterMenuSection>
    <HeaderFooterMenuSection title={args.title}>
      {args.children}
    </HeaderFooterMenuSection>
  </HeaderFooterMenu>
);

# HeaderFooterMenu

<Canvas>
  <Story
    name="HeaderFooterMenu with accordion on mobile"
    parameters={{
      viewport: {
        defaultViewport: 'sm',
      },
    }}
    args={{
      allowMultiple: true,
      allowToggle: false,
      title: 'Section title',
      children: (
        <List>
          <ListItem>Item 1</ListItem>
          <ListItem>Item 2</ListItem>
          <ListItem>Item 3</ListItem>
        </List>
      ),
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
