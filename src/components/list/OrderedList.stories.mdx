import { Canvas, Meta, Story } from '@storybook/addon-docs';

import Box from 'src/components/box';

import OrderedList from './OrderedList';
import ListItem from './ListItem.tsx';

<Meta
  title="Components/Data display/List/Ordered list"
  component={OrderedList}
  parameters={{
    layout: 'fullscreen',
  }}
  args={{
    size: 'md',
    variant: 'icon-inside',
    start: 1,
  }}
  argTypes={{
    size: {
      options: ['sm', 'md'],
      control: 'select',
    },
    variant: {
      options: ['icon-inside', 'icon-outside'],
      control: 'select',
    },
  }}
/>

export const Template = (args) => (
  <Box maxW="200px" border="1px solid black">
    <OrderedList {...args} bg="gray.100">
      <ListItem>
        Item 1 is a very long item that will wrap to the next line
      </ListItem>
      <ListItem>Item 2</ListItem>
      <ListItem>Item 3</ListItem>
    </OrderedList>
  </Box>
);

# List

## Overview

<Canvas>
  <Story name="Ordered list">{Template.bind({})}</Story>
</Canvas>
