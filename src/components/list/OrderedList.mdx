import { Controls, Canvas, Meta, Story } from '@storybook/blocks';
import * as OrderedListStories from './OrderedList.stories.tsx';

<Meta of={OrderedListStories} />

# List

## Overview

Use `showContainer` to see the difference between `icon-inside` and `icon-outside` variants.

<Canvas>
  <Story of={OrderedListStories.OrderedList} />
</Canvas>

<Controls />
