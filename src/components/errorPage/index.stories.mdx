import { Canvas, Meta, Story } from '@storybook/addon-docs';

import ErrorPage from './index.tsx';
import { AutoScout24Logo } from '../icons';

<Meta title="Patterns/Content/Error" component={ErrorPage} />

export const Template = (args) => <ErrorPage {...args} />;

# Error page

## Overview

<Canvas>
  <Story
    name="Overview"
    args={{
      statusCode: 404,
      language: 'de',
      brandLogo: <AutoScout24Logo width="150px" height="34px" />,
    }}
    argTypes={{
      statusCode: {
        options: [404, 500],
        control: 'select',
      },
      language: {
        options: ['de', 'fr', 'it', 'en'],
        control: 'select',
      },
      brandLogo: {
        table: {
          disable: true,
        },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
