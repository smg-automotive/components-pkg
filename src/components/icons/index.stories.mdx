import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { Box, Center } from '@chakra-ui/react';

import SimpleGrid from '../simpleGrid';

import * as Icons from './index';

<Meta
  title="Foundations/Icons"
  parameters={{
    docs: {
      source: {
        code: null,
      },
    },
  }}
/>

export const Template = () => {
  return (
    <SimpleGrid columns={6} spacing="lg">
      {Object.keys(Icons)
        .sort()
        .map((icon, index) => (
          <Box
            key={index}
            borderWidth="1px"
            borderRadius="lg"
            borderColor="gray.200"
            p="md"
          >
            <Center>{React.createElement(Icons[icon])}</Center>
            <Center>
              <p>{icon}</p>
            </Center>
          </Box>
        ))}
    </SimpleGrid>
  );
};

# Icons

## Overview

<Canvas>
  <Story name="Icons">{Template.bind({})}</Story>
</Canvas>
