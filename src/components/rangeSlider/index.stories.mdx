import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';

import RangeSlider from './index.tsx';

<Meta title="Components/Filter/Range Slider" component={RangeSlider} />;

export const Template = (args) => {
  return (
    <RangeSlider
      onChange={(event) => {
        action('change')(event);
      }}
      onChangeEnd={(event) => {
        action('change end')(event);
      }}
      {...args}
    />
  );
};

## Overview

<Canvas>
  <Story name="Overview">{Template.bind({})}</Story>
</Canvas>
