import { Canvas, Meta, Story } from '@storybook/addon-docs';

import Breadcrumbs from './index.tsx';
import BreadcrumbLink from './BreadcrumbLink';
import BreadcrumbItem from './BreadcrumbItem';

import { Divider, Hide, Box, Center } from '@chakra-ui/react';

import Link from '../link';
import { ArrowLeftIcon as BackArrowIcon } from '../icons';

<Meta title="Patterns/Navigation/Breadcrumbs" component={Breadcrumbs} />

export const Template = (args) => (
  <Box display="flex">
    {args?.backButton && (
      <Link leftIcon={<BackArrowIcon />} href={args.backButton}>
        Back
      </Link>
    )}
    <Hide below="sm">
      <Center height="sm" mr="md" ml="md">
        <Divider orientation="vertical" />
      </Center>
      <Breadcrumbs>
        {args?.crumbs.map((crumb, i) => (
          <BreadcrumbItem key={i}>
            <BreadcrumbLink href={crumb?.url}>{crumb.title}</BreadcrumbLink>
          </BreadcrumbItem>
        ))}
      </Breadcrumbs>
    </Hide>
  </Box>
);

# Breadcrumbs

## Overview

<Canvas>
  <Story
    name="Overview"
    args={{
      crumbs: [
        { title: 'VW', url: '#' },
        { title: 'Golf', url: '##' },
        { title: 'VW GOLF 2.0 TSI GTI DSG Performance' },
      ],
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## With Back Button

<Canvas>
  <Story
    name="With Back Button"
    args={{
      backButton: '#',
      crumbs: [
        { title: 'VW', url: '#' },
        { title: 'Golf', url: '##' },
        { title: 'VW GOLF 2.0 TSI GTI DSG Performance', url: '###' },
      ],
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
