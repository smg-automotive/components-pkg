import { Canvas, Meta, Story } from '@storybook/addon-docs';

import Breadcrumbs from './index.tsx';
import BreadcrumbItem from './BreadcrumbItem';
import BreadcrumbBackButton from './BreadcrumbBackButton';
import BreadcrumbLink from './BreadcrumbLink';

import Link from '../link';

<Meta title="Patterns/Navigation/Breadcrumbs" component={Breadcrumbs} />

export const Template = (args) => (
  <Breadcrumbs>
    {args?.backButton && <BreadcrumbBackButton href={args.backButton} />}
    {args?.crumbs.map((crumb, i) => (
      <BreadcrumbItem key={i}>
        <BreadcrumbLink isLast={args.crumbs.length - 1 === i} href={crumb.url}>
          {crumb.title}
        </BreadcrumbLink>
      </BreadcrumbItem>
    ))}
  </Breadcrumbs>
);

# Breadcrumbs

## Overview

<Canvas>
  <Story
    name="Overview"
    args={{
      crumbs: [
        { title: 'VW', url: '#' },
        { title: 'Golf', url: '##' },
        { title: 'VW GOLF 2.0 TSI GTI DSG Performance', url: '###' },
      ],
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## With Back Button

<Canvas>
  <Story
    name="With Back Button"
    args={{
      backButton: '#',
      crumbs: [
        { title: 'VW', url: '#' },
        { title: 'Golf', url: '##' },
        { title: 'VW GOLF 2.0 TSI GTI DSG Performance', url: '###' },
      ],
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
