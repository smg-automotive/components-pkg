import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { Box } from '@chakra-ui/react';

import Breadcrumbs from './index.tsx';

<Meta title="Components/Navigation/Breadcrumbs" component={Breadcrumbs} />

export const Template = ({ crumbs, backLink, LinkWrapper }) => (
  <Breadcrumbs crumbs={crumbs} backLink={backLink} LinkWrapper={LinkWrapper} />
);

# Breadcrumbs

## Overview

<Canvas>
  <Story
    name="Overview"
    args={{
      backLink: { title: 'Back', url: '#' },
      crumbs: [
        { title: 'VW', url: '#' },
        { title: 'Golf', url: '###' },
        { title: 'VW GOLF 2.0 TSI GTI DSG Performance' },
      ],
      LinkWrapper: Box,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Overview" />
