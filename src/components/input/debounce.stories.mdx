import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import { action } from '@storybook/addon-actions';

import Input from './index.tsx';
import { Template } from './index.stories.mdx';

<Meta
  title="Components/Forms/Input/Debounced"
  component={Input}
  parameters={{
    controls: {
      sort: 'alpha',
      expanded: true,
    },
    actions: ['setInputvalue'],
  }}
/>

# Input

## Debounced

<Canvas>
  <Story
    name="Default"
    args={{
      size: 'lg',
      placeholder: 'Placeholder',
      isDisabled: false,
      isInvalid: false,
      autoFocus: false,
      debounce: true,
      setInputValue: action('setInputvalue'),
    }}
    argTypes={{
      size: {
        options: ['md', 'lg'],
        control: 'select',
      },
      isDisabled: {
        table: {
          disable: true,
        },
      },
      isInvalid: {
        table: {
          disable: true,
        },
      },
      autoFocus: {
        table: {
          disable: true,
        },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
