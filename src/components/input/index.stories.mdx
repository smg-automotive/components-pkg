import { Canvas, Meta, Story } from '@storybook/addon-docs';

import Input from './index.tsx';

<Meta title="Components/Input" component={Input} />

export const Template = (args) => <Input {...args} />;

# Input

## Overview

<Canvas>
  <Story name="Overview" args={{}} argTypes={{}}>
    {Template.bind({})}
  </Story>
</Canvas>
