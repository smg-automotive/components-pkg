import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { Box, Flex } from '@chakra-ui/react';

<Meta title="Theme" component={Box} />

# Theme

## Colors palette

<Canvas>
  <Story name="colors">
    {() => {
      return (
        <Flex direction="row" align="center" justify="left" wrap="wrap">
          <Flex direction="column" align="center" justify="left" mr={5}>
            {[
              '50',
              '100',
              '200',
              '300',
              '400',
              '500',
              '600',
              '700',
              '800',
              '900',
            ].map((shade) => {
              return (
                <Flex
                  direction="row"
                  align="center"
                  justify="space-between"
                  key={`brand-${shade}`}
                >
                  <Box
                    bg={`brand.${shade}`}
                    h={50}
                    w={50}
                    mr={3}
                    mb={3}
                    borderRadius={50}
                    borderWidth={1}
                    borderStyle="solid"
                    borderColor="gray.500"
                  />
                  <Box w={100}>{`brand-${shade}`}</Box>
                </Flex>
              );
            })}
          </Flex>
        </Flex>
      );
    }}
  </Story>
</Canvas>

<ArgsTable story="colors" />
